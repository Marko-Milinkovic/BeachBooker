# BeachBooker — Python dependencies
#
# Full setup: see README.md
#
# First-time clone:
#   pip install -r requirements.txt
#   copy .env.example .env
#   # Import database/schema.sql in MySQL Workbench, then:
#   python manage.py migrate --fake-initial
#   python manage.py migrate
#   python manage.py runserver
#
# After git pull (required when core/migrations/ changes):
#   git pull
#   pip install -r requirements.txt
#   python manage.py migrate
#
# Old schema.sql without user.last_login? migrate adds it (core.0002) — no re-import needed.
#
# Windows: if mysqlclient install fails, pip installs PyMySQL automatically.

Django>=4.2,<6
python-dotenv>=1.0,<2
selenium>=4.15,<5

# MySQL driver — mysqlclient on Linux/macOS; PyMySQL fallback on Windows
mysqlclient>=2.2,<3; platform_system != "Windows"
PyMySQL>=1.1,<2; platform_system == "Windows"
